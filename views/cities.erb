<p>Add an city to the database:</p>

<form action="/cities" method="post">
  <label for="city_name">Actor's name:</label>
  <input id="city_name" name="city_name" type="text">

  <button type="submit">Add city</button>
</form>


<% if @cities.any?() %>
  <p>Click on an city to see information about it and to assign it to different trains:</p>

  <ul>
    <% @cities.each do |city| %>
      <li><a href="/cities/<%= city.id() %>"><%= city.name() %></a></li>
    <% end %>
  </ul>
<% else %>
  <p>There are no cities added yet!</p>
<% end %>

<p><a href="/trains">See all trains</a></p>

<p><a href="/">Return to Main Page</a></p>
